import IndexingPage from '@components/Indexing'
import { FC } from 'react'

interface PageProps {

}

const Page: FC<PageProps> = ({ }) => {
    return <div>
        <IndexingPage />
    </div>
}

export default Page
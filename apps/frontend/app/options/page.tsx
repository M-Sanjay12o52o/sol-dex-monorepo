import OptionsPage from '@components/Options'
import { FC } from 'react'

interface PageProps {

}

const Page: FC<PageProps> = ({ }) => {
    return <div>
        <OptionsPage />
    </div>
}

export default Page